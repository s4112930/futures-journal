# Futures Journal 專案治理

- 專案：期貨分析系統（Futures Journal）
- 儲存庫：`s4112930/futures-journal`
- 後端原始儲存庫：`s4112930/futures-ai-worker`（整合完成後改為私人並封存）
- 正式網站：`https://s4112930.github.io/futures-journal/`
- 治理版本：1.1.0
- 最後更新：2026-08-29
- 適用範圍：本儲存庫根目錄及所有子目錄

## 1. 唯一主線與資料來源

1. `main` 分支是唯一正式主線，只保留可部署、可回復的版本。
2. 本檔案是跨對話共同遵守的專案治理規則。
3. GitHub Issues 是任務名稱、優先順序、狀態與驗收條件的唯一來源。
4. Pull Request、commit 與程式碼是實際變更紀錄。
5. 對話內容只用於討論，不視為永久紀錄；重要決定必須寫回 Issue、PR 或本檔案。

資料衝突時依序採用：

1. 使用者在目前任務的明確指示
2. 本治理檔案
3. 該任務的 GitHub Issue 與最新留言
4. 已合併的程式碼、測試及文件
5. 舊對話或未寫入 GitHub 的敘述

## 2. 每個新對話的啟動流程

開始任何規劃或修改前，必須依序：

1. 開啟並同步 `s4112930/futures-journal` 的最新 `main`。
2. 完整讀取根目錄 `AGENTS.md`。
3. 查看儲存庫結構、目前分支、`git status` 及最近提交，不憑舊對話猜測現況。
4. 讀取指定的 GitHub Issue，包括驗收條件與最新留言。
5. 用一句話確認：任務編號、目標、預計修改範圍及驗證方式。
6. 未取得足夠資料時，標示「尚未確認」並先查證，不自行補造設定、數據或完成狀態。

若使用者沒有指定 Issue：

- 僅諮詢、說明或除錯診斷可以直接進行，但不得自行擴大成程式修改。
- 需要修改程式時，先建立或指定一個 Issue，再開始實作。

## 3. 對話角色

### 主線治理對話

- 維護專案目標、架構、共通規則與重大決策。
- 只有規則確實改變時才更新 `AGENTS.md`。
- 不把每日工作紀錄堆進本檔案。

### 任務管理對話

- 使用 GitHub Issues 建立、排序、拆分及追蹤任務。
- 確認每個 Issue 有明確範圍、驗收條件、優先級與狀態。
- 不直接修改產品程式碼，除非使用者明確指定同時實作。

### 任務執行對話

- 一個對話原則上只處理一個 Issue。
- 僅修改該 Issue 必要的檔案。
- 完成後留下可供下一個對話接手的 GitHub 紀錄。

## 4. GitHub 任務規格

每個 Issue 至少包含：

```md
## 目標
一句話說明要解決的問題。

## 現況／證據
目前實際行為、畫面、錯誤訊息或可重現步驟。

## 範圍
- 要做：
- 不做：

## 驗收條件
- [ ] 可客觀確認的結果 1
- [ ] 可客觀確認的結果 2

## 風險或相依任務
沒有則填「無」。
```

建議標籤：

- 類型：`type:feature`、`type:bug`、`type:refactor`、`type:docs`
- 狀態：`status:backlog`、`status:ready`、`status:in-progress`、`status:review`、`status:blocked`
- 優先級：`priority:P0`、`priority:P1`、`priority:P2`、`priority:P3`

同一時間只保留一個狀態標籤。Issue 關閉代表已完成且通過驗收。

## 5. 分支與合併規則

- 禁止直接在 `main` 上開發未驗證功能。
- 功能分支：`feature/issue-<編號>-<短名稱>`
- 修正分支：`fix/issue-<編號>-<短名稱>`
- 文件或治理：`docs/issue-<編號>-<短名稱>`
- 每個分支只對應一個 Issue；提交應小而清楚。
- 合併前必須檢查差異、通過適用驗證，並確認沒有 API 金鑰、密碼或個資。
- 未經使用者明確要求，不改動 GitHub Pages 部署來源、網域或公開設定。
- 合併後刪除已完成的工作分支；保留 `main` 與 GitHub 歷史即可回溯。

建議 commit 格式：

```text
feat: 完成月份篩選 (#12)
fix: 修正最近交易期間不同步 (#13)
docs: 更新跨對話治理規則 (#14)
```

## 6. 任務交接紀錄

任務執行對話結束前，必須在 Issue 或 PR 留下：

```md
## 交接摘要
- 完成內容：
- 修改檔案：
- 驗證結果：
- 尚未完成：
- 已知風險：
- 建議下一步：
```

不得只寫「已完成」。若未完成，必須說明卡點與目前可重現狀態。

## 7. 已確認的產品需求

- 產品是個人使用的期貨交易紀錄與分析網頁。
- 期間選項包含：
  1. 單月份
  2. 近三個月
  3. 近半年
- 選擇單月份時，統計與「最近交易」只顯示該月份交易。
- 選擇近三個月或近半年時，統計與交易清單必須使用相同期間，不得各自計算。
- AI 分析要根據該筆交易已有資料，整理獲利或虧損的可能原因。
- AI 分析必須區分「資料可直接證明」與「可能原因」；缺少交易資料時要明說，不能把推測寫成事實。
- 使用者介面以手機網頁可用為必要條件。
- 回覆程式修改時，以簡潔說明搭配完整可替換程式碼；不得只給無法定位的片段。

尚未確認、不得自行假設：

- TradingView 要採用哪一種串接方式、帳號權限與資料來源。
- AI 分析使用的模型、後端、API 供應商與費用方案。
- 目前儲存庫的實際檔案架構、資料格式及部署工作流程；首次接入後必須由程式碼驗證。

## 8. 目標架構與隱私決策

- 2026-08-29 已確認：網站、IG 同步與 AI 分析最終只能由使用者本人登入使用。
- 目標以 `s4112930/futures-journal` 作為唯一私人主儲存庫，整合前端、Worker、資料庫遷移及共同治理。
- `s4112930/futures-ai-worker` 只作為遷移來源；新架構驗證成功後改為私人並封存，不直接刪除歷史。
- 正式網站由 Cloudflare Worker Static Assets 與同一 Worker API 提供，避免前後端跨網域及重複部署。
- 網站與 API 必須放在 Cloudflare Access 或等效的伺服器端身分驗證後；CORS 只能限制瀏覽器跨來源讀取，不視為登入或存取控制。
- 交易資料目標移至 Cloudflare D1；瀏覽器 `localStorage` 僅作為遷移來源或暫存，不作為唯一永久資料庫。
- 改變網站來源前，必須提供完整 JSON 匯出、備份及新系統匯入驗證。不同網域不能直接共用現有 `localStorage`。
- 在資料匯出、新部署、本人登入、IG 同步與資料匯入全部驗證成功前，不得先把 `futures-journal` 改為私人或停止現有 GitHub Pages。
- 統計指標由確定性程式計算；AI 只能解釋已計算的結果與已提供紀錄，不負責憑空計算或補造市場原因。

## 9. 資料、AI 與交易分析原則

- 不捏造交易紀錄、技術指標、進出場理由、行情事件或報酬原因。
- 分析只能使用實際輸入或已成功取得的資料。
- 推測必須標示為「可能原因」，並列出支持或反駁它所需的資料。
- 不把統計相關誤寫成因果關係。
- 涉及即時行情、新聞、法規、產品規格或 API 能力時，必須查閱當下可靠來源。
- 任何交易分析都屬輔助資訊，不得以保證獲利、必然上漲或必然下跌表述。

## 10. 安全與隱私

- 不得把 API key、token、密碼、Cookie 或私人交易資料提交到 GitHub。
- GitHub Pages 的前端程式碼與網路請求可被查看，不能在前端保存秘密金鑰。
- 需要 AI 或第三方服務金鑰時，必須使用可保護秘密的後端或安全代理；在架構未確認前不得硬接到公開前端。
- 所有 IG 帳戶、交易與 AI 端點都必須驗證使用者身分；不得只依賴 `Origin` 或 `Access-Control-Allow-Origin`。
- 正式環境不得保留可匿名呼叫的 IG 登入測試或秘密存在狀態端點。
- Worker 必須啟用適用的日誌與錯誤觀測，但不得將 IG token、密碼、API key 或完整私人交易內容寫入日誌。
- 不刪除或覆蓋既有交易資料，除非使用者明確要求且已說明備份或復原方式。
- 修改既有儲存格式前，先檢查相容性並提供必要的資料遷移。

## 11. 完成定義

任務只有同時符合下列條件才可標示完成：

1. Issue 的每項驗收條件均已確認。
2. 手機與桌面主要流程可操作。
3. 適用的測試、建置或人工檢查已完成，且瀏覽器主控台無新增錯誤。
4. 期間篩選、統計與交易清單使用同一資料範圍。
5. 沒有提交秘密資訊或破壞既有資料。
6. PR 或 Issue 已留下交接摘要。
7. 變更已合併至 `main`；若尚未合併，只能標示「待審查」。

## 12. 本次可驗證狀態

- 已連接 `s4112930/futures-journal`，儲存庫目前為公開，預設分支是 `main`。
- 已連接 `s4112930/futures-ai-worker`，儲存庫目前為公開，預設分支是 `main`，根目錄只有 `worker.js` 與 `wrangler.jsonc`。
- 2026-08-29 盤點時只有 `main` 分支，根目錄包含 `README.md` 與 `index.html`，尚無未關閉 Issues。
- 現行前端集中在單一 `index.html`，交易資料儲存在瀏覽器 `localStorage` 的 `futuresTrades`。
- 現行 AI 分析由前端呼叫 `https://futures-ai-worker.s4112930.workers.dev`；Worker 原始碼不在本儲存庫內，能力與安全設定須另行查證。
- 目前 Worker 的 IG 帳戶、交易及 AI 路由沒有伺服器端使用者驗證；固定 CORS 來源不構成身分驗證，列為最高優先安全修正。
- 目前 IG 交易同步固定使用 `pageSize=100` 與 `pageNumber=1`，尚未實作分頁；超過 100 筆時可能無法完整同步。
- 目前 AI 只分析單筆交易，尚未建立跨交易的操作型態、風險、回撤與策略績效分析。
- 已知公開網站為 `https://s4112930.github.io/futures-journal/`；本次未讀取 GitHub Pages 設定，因此不自行宣稱其實際部署來源或更新流程。
- 2026-08-29 建立治理檔案第一版；後續以 GitHub Issue、文件分支與 Pull Request 納入 `main`。

## 13. 新對話啟動指令

任務管理對話可使用：

```text
請連接 GitHub 儲存庫 s4112930/futures-journal，先完整讀取根目錄 AGENTS.md，依治理規則擔任任務管理者。先同步 main、盤點未關閉 Issues 與最近變更，再列出目前優先順序；除非我明確要求，先不要修改產品程式碼。
```

任務執行對話可使用：

```text
請連接 GitHub 儲存庫 s4112930/futures-journal，先完整讀取根目錄 AGENTS.md，再處理 Issue #<編號>。先確認驗收條件與目前程式碼，建立符合命名規則的分支，完成後驗證並留下交接摘要；不要擴大任務範圍。
```

## 14. 治理檔更新規則

- 只有長期適用的規則、已確認產品需求、架構決策或安全限制才寫入本檔案。
- 每日進度、暫時想法與單一任務細節留在 Issue 或 PR。
- 修改本檔案必須說明修改原因；重大規則變更需由使用者確認。
- 發現本檔案與實際程式碼不一致時，不得偷偷選一邊；先提出差異，再修正權威來源。
